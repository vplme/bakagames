name: bird_sort
description: Bird Sort engine, solver, generator and level models (pure Dart, no Flutter).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
