name: game_core
description: Shared shell contracts for the puzzle-games app (pure Dart, no Flutter).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
